<div>
	<% form_tag({:action => ''}) do %>
		<strong>Search:</strong> <%= text_field_tag 'query', '', options = {:value => @query, :size => 25, :maxlength => 25} %>		
		<%= submit_tag("Go!") %>
	<% end %>
</div>

<h3>Files</h3>

<% if @files %>
	<% @files.each do |f| %>
	<div>					
	  <%= f.dirname %> | <%= link_to f.filename, :action => 'download', :id => f.id %> | <%= f.filesize %>
	</div>
	<% end %>
</div>
<% end %>